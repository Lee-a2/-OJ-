# 项目接口测试文档

本文档详细描述了 Java Online Judge (OJ) 系统的后端接口，包括接口地址、请求方式、参数说明及预期响应结果。

## 目录

1. [用户注册 (/register)](#1-用户注册-register)
2. [用户登录 (/login)](#2-用户登录-login)
3. [获取用户信息 (/admin)](#3-获取用户信息-admin)
4. [注销/删除用户 (/logout)](#4-注销删除用户-logout)
5. [新增题目 (/addProblem)](#5-新增题目-addproblem)
6. [获取题目列表/详情 (/problem)](#6-获取题目列表详情-problem)
7. [编译运行代码 (/compile)](#7-编译运行代码-compile)
8. [删除题目 (/deleteProblem)](#8-删除题目-deleteproblem)

---

### 1. 用户注册 (/register)

- **URL**: `/register`
- **Method**: `POST`
- **Content-Type**: `application/x-www-form-urlencoded`
- **功能描述**: 注册新用户，可选注册为管理员。

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `username` | String | 是 | 用户名 |
| `password` | String | 是 | 密码 |
| `admin` | String | 否 | 传入 "1" 则注册为管理员，否则为普通用户 |

**预期响应 (成功)**:
- 页面重定向至登录页或提示注册成功 HTML。

---

### 2. 用户登录 (/login)

- **URL**: `/login`
- **Method**: `POST`
- **Content-Type**: `application/x-www-form-urlencoded`
- **功能描述**: 用户登录验证。

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `username` | String | 是 | 用户名 |
| `password` | String | 是 | 密码 |

**预期响应 (成功)**:
- 页面重定向至主页 (`/`) 或提示登录成功 HTML。

---

### 3. 获取用户信息 (/admin)

- **URL**: `/admin`
- **Method**: `GET`
- **功能描述**: 获取当前登录用户的详细信息。通常用于前端判断用户权限（是否为管理员）。

**请求参数**: 无

**预期响应 (成功)**:
```json
{
    "id": 1,
    "username": "admin",
    "password": "...",
    "isAdmin": 1
}
```

---

### 4. 注销/删除用户 (/logout)

- **URL**: `/logout`
- **Method**: `GET` / `POST`
- **功能描述**:
    - `GET`: 注销当前登录状态。
    - `POST`: 删除指定用户（需要管理员权限或特定逻辑，视实现而定）。

**请求参数 (POST)**:
- `username`: 要删除的用户名。

**预期响应 (成功)**:
- 重定向至登录页 (`login.html`)。

---

### 5. 新增题目 (/addProblem)

- **URL**: `/addProblem`
- **Method**: `POST`
- **Content-Type**: `application/json`
- **功能描述**: 管理员添加新的编程题目。

**请求参数示例**:
```json
{
    "id": 1,
    "title": "两数之和",
    "level": "简单",
    "description": "计算两个整数的和",
    "templateCode": "class Solution { ... }",
    "testCode": "public static void main(String[] args) { ... }"
}
```

**预期响应 (成功)**:
```json
{
    "correct": 1
}
```

---

### 6. 获取题目列表/详情 (/problem)

- **URL**: `/problem`
- **Method**: `GET`
- **功能描述**: 获取题目列表或单个题目的详细信息。

**场景 A: 获取题目列表**
- **请求参数**: 无
- **预期响应**:
```json
[
    {
        "id": 1,
        "title": "两数之和",
        "level": "简单",
        "description": "...",
        "templateCode": "...",
        "testCode": "..."
    },
    {
        "id": 2,
        "title": "...",
        ...
    }
]
```

**场景 B: 获取单个题目详情**
- **请求参数**: `id` (例如 `?id=1`)
- **预期响应**:
```json
{
    "id": 1,
    "title": "两数之和",
    "level": "简单",
    "description": "...",
    "templateCode": "...",
    "testCode": "..."
}
```

---

### 7. 编译运行代码 (/compile)

- **URL**: `/compile`
- **Method**: `POST`
- **Content-Type**: `application/json`
- **功能描述**: 提交用户代码，在服务端编译并运行，返回结果。

**请求参数示例**:
```json
{
    "id": 1,
    "code": "public class Solution { ... }"
}
```

**预期响应 (成功)**:
```json
{
    "error": 0,
    "reason": "",
    "stdout": "Test Passed"
}
```
*注: `error` 字段说明: 0=成功, 1=编译错误, 2=运行错误, 3=其他错误*

---

### 8. 删除题目 (/deleteProblem)

- **URL**: `/deleteProblem`
- **Method**: `POST`
- **Content-Type**: `application/json`
- **功能描述**: 管理员删除指定题目。

**请求参数示例**:
```json
{
    "id": 1
}
```

**预期响应 (成功)**:
```json
{
    "result": 1
}
```
